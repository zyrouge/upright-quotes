export class ExternalURLs {
    static _ghUsername = "zyrouge";
    static _ghRepo = "upright-quotes";
    static _ghDistBranch = "gh-pages";
    static github = `https://github.com/${ExternalURLs._ghUsername}/${ExternalURLs._ghRepo}`;
}
